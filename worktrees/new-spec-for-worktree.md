# New Spec for Worktree

Argument: spec name

Create a file called ./specs/{spec-name}.md.
Then, ask the user to give more details about what they want in the spec, and work the with user to develop the spec and make sure their idea is well formed.
Suggest that the next command to run once the spec is done should be /new-worktree
